this folder contains all the page components files where each page's components will be fetched from
(components being more than just single tags but combinations of them to make entire components
of each page); this might change - we might just dump all the components inside each page's file
itself